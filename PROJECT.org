#+TITLE: Project — Actionable Work
#+AUTHOR: James Cook
#+STARTUP: overview
#+TODO: TODO(t) IN-PROGRESS(i) BLOCKED(b) | DONE(d) REJECTED(r)
:PROPERTIES:
:ID:       project-root
:END:

Actionable tracking, separate from [[file:docs/roadmap.org][docs/roadmap.org]] (which is the phased
narrative). This file is meant to be Agenda-includable later without restructuring
— see the Documentation acceptance test in [[file:docs/ai/evaluation.org][docs/ai/evaluation.org]].

* Phase 1 — AI integration

** DONE Phase 0 — Read-only discovery
CLOSED: [2026-07-20 Mon]
:PROPERTIES:
:EFFORT:   1h
:END:

** DONE Phase 1 — Architecture proposal
CLOSED: [2026-07-20 Mon]
Decisions recorded in [[file:docs/decisions.org][docs/decisions.org]] ADR-003, ADR-004, ADR-005.

** DONE Phase 2 — Baseline modernization
CLOSED: [2026-07-20 Mon]
:PROPERTIES:
:EFFORT:   1h
:END:
Doom core upgraded, EXWM removed, =doom-dashboard= rename. See ADR-001, ADR-002.

** DONE Phase 3 — Documentation foundation
CLOSED: [2026-07-20 Mon]
:PROPERTIES:
:EFFORT:   1h
:END:
- [X] docs/architecture.org
- [X] docs/standards.org
- [X] docs/inventory.org
- [X] docs/roadmap.org
- [X] docs/decisions.org
- [X] docs/ai/architecture.org
- [X] docs/ai/workflows.org
- [X] docs/ai/keybindings.org
- [X] docs/ai/providers.org
- [X] docs/ai/prompts.org
- [X] docs/ai/evaluation.org
- [X] docs/ai/troubleshooting.org
- [X] README.org
- [X] CLAUDE.md
- [X] AGENTS.md
- [X] .claude/rules/doom-emacs.md
- [X] .claude/rules/security.md
- [X] .claude/rules/documentation.md
- [X] prompts/phase-01-ai-integration.md (moved from repo root)

** DONE Phase 4 — Chat and focused assistance
CLOSED: [2026-07-20 Mon]
:PROPERTIES:
:EFFORT:   2h
:END:
- [X] Create =api/anthropic= pass entry (user action); Google skipped per
  user decision, no =api/gemini= needed
- [X] Migrate credential access to Doom =:tools pass= =+auth=, remove
  =my/pass-get=
- [X] Configure =gptel-make-anthropic=/=-openai=, Anthropic as default backend
  (Gemini explicitly skipped)
- [X] Replace =my/gptel-rewrite-*=/=my/gptel-send-buffer= with built-in
  =gptel-rewrite=/=gptel-add= + =gptel-directives= entries
- [X] Flatten =SPC a= to the AI namespace (ADR-003), fixing a discovered
  dead-code bug in the original reclamation ([[file:docs/ai/troubleshooting.org][troubleshooting.org]])
- [X] Verify backend/model/credential/keybinding wiring via isolated headless
  daemon
- [X] Test in the user's own interactive session: streaming, cancellation,
  popup behavior, Org-buffer chat UX, network-down behavior — user confirmed
  all working

** DONE Phase 5 — Inline completion
CLOSED: [2026-07-20 Mon]
:PROPERTIES:
:EFFORT:   1h
:END:
- [X] Verify copilot-language-server install path: npm path confirmed (system
  npm found), server v1.452.0, no =engines= constraint — Node 18.19.1 concern
  resolved
- [X] Live-test =TAB= precedence: Copilot overlay vs. Company vs. Evil vs.
  yasnippet, all verified via simulated overlays in disposable isolated
  daemons (user's own daemon never touched beyond read-only =ps=/=/proc=)
- [X] Confirm next-edit-suggestion behavior: =copilot-nes-mode= exists,
  compatible (min server 1.434.0, installed 1.452.0), enabled, keymap
  fallthrough verified
- [X] Removed a redundant no-op =:bind= block in =config.el=
- [X] User's own interactive/authenticated session: a live suggestion/NES-edit
  appeared and was accepted — user confirmed it worked nicely

** DONE Phase 6 — Primary agentic workflow
CLOSED: [2026-07-20 Mon]
:PROPERTIES:
:EFFORT:   3h
:END:
- [X] Install claude-code-ide.el ([[id:adr-primary-agent][ADR-004]], implemented [[id:adr-claude-code-ide-install][ADR-008]])
- [X] Verify CLI detection (local, network-free =claude --version= check)
- [X] Verify tree-sitter MCP tool given [[id:tshoot-treesitter-confirmed][the tree-sitter issue]] — *confirmed*
  reproduced on this exact Emacs 30.1 build (not just theoretical), plus a
  separate finding that no grammars were installed at all
- [X] Full project workflow test (plan → edit → test → diff → no commit) —
  user approved, real live smoke test run against a disposable scratch
  project: seeded bug found and fixed, tests run, no git actions, clean
  teardown. Also surfaced and fixed a real vterm-module compile gap
  ([[id:tshoot-vterm-module][troubleshooting.org]]) that would have blocked this in the user's own
  session too
- [ ] Remote/TRAMP test against an Armbian/SBC target — needs actual remote
  hardware not available to this agent; only genuinely deferred item

** DONE Phase 7 — Review, permissions, safety
CLOSED: [2026-07-21 Tue]
:PROPERTIES:
:EFFORT:   1h
:END:
- [X] Confirm Ediff review path end-to-end — user ran the live test script
  (real session, multi-hunk diff, Ediff accept/reject/edit, Magit accuracy,
  diagnostics and tests re-run from Emacs, rollback) after fixing the
  daemon-PATH issue found along the way ([[id:tshoot-daemon-path-systemd][troubleshooting.org]]) via =M-x
  doom/reload-env=. See [[file:docs/ai/evaluation.org][evaluation.org]] "Review (Phase 7)" checklist, all
  checked.
- [X] Look for Claude Code permission/hook mechanisms to enforce no-commit/no-push
  technically — implemented via a committed =.claude/settings.json=
  =permissions.deny= list blocking =git commit=/=push=/=rebase=/=reset=/=clean=
  outright, live-verified. See [[id:adr-permission-enforcement][ADR-009]] in [[file:docs/decisions.org][decisions.org]].

** DONE Phase 8 — Prompts, memory, skills
CLOSED: [2026-07-21 Tue]
:PROPERTIES:
:EFFORT:   1h
:END:
- [X] Finalize =CLAUDE.md=/=AGENTS.md= once Phase 4-6 commands exist to
  document — added =docs/ai/architecture.org=/=workflows.org= pointers and an
  ADR-009 cross-reference to =AGENTS.md='s "Where to look first"/Hard rules;
  both files stay well under the 200-line policy (54 + 15 lines). =CLAUDE.md=
  needed no changes.
- [X] Decide on a skill policy if repeatable workflows emerge — user chose to
  build a lightweight ADR-scaffold skill (=.claude/skills/new-adr/=, =/new-adr=)
  since =decisions.org='s convention outlasts Phase 1; deferred tooling for
  the headless-daemon-test and phase-closeout patterns as Phase-1-specific.
  See [[id:adr-skill-policy][ADR-010]] in [[file:docs/decisions.org][decisions.org]] and the new "Skill policy" section in
  [[file:docs/ai/prompts.org][prompts.org]].

** DONE Phase 9 — Validation and handoff
CLOSED: [2026-07-21 Tue]
:PROPERTIES:
:EFFORT:   1h
:END:
- [X] Run all acceptance tests in [[file:docs/ai/evaluation.org][docs/ai/evaluation.org]] — every item
  realistically testable from this project's own position is now checked
  (retroactive closures from earlier user confirmations, structural/source-
  based reasoning where live-testing wasn't feasible, one fresh headless
  test for Org-Agenda compatibility, and a re-run ID/link integrity check —
  69 IDs, 0 duplicates, 0 broken links). User then live-tested the last two
  testable items: interrupting the agent (works) and clicking file
  references in chat output (never a real feature to begin with — see
  [[id:tshoot-file-refs-mis-scoped][troubleshooting.org]]). Three items remain unchecked, all genuinely
  hardware-gated (remote/TRAMP, a second machine, disconnecting this
  machine's network) — nothing left that's actually actionable.
- [X] Final docs pass — refreshed several stale claims found along the way
  (credential-state table, Phase 4/5 "not yet verified" notes that were
  actually already confirmed, evaluation.org's Phase-3-era intro line),
  fleshed out =inventory.org='s binary lifecycle/reproduction-checklist
  gaps, and re-validated ID/link integrity across the whole tree.
- [X] Clean diff presented for user commit — =git status=/=git diff=
  reviewed, nothing unintended, no secret values present. Ready for your
  review.

* DONE Phase 3 — RSS integration
CLOSED: [2026-08-10 Mon]
:PROPERTIES:
:EFFORT:   30m
:END:
Promoted from [[file:docs/roadmap.org][docs/roadmap.org]]'s old bundled "RSS and communications" Future
bullet — RSS only, scoped via a grill session. See [[file:docs/decisions.org][docs/decisions.org]]
ADR-012 for the full decision record.
- [X] =init.el=: enable =:app (rss +org +youtube)=
- [X] =config.el=: =SPC o r= leader binding, auto-update-on-open hook
- [X] Scaffold =~/org/elfeed.org= with just the required =:elfeed:=-tagged
  root headline (real feeds added later by the user)
- [X] =doom sync= clean (188 packages, elfeed/elfeed-goodies/elfeed-org/
  elfeed-tube all installed)
- [X] =doom doctor= run — clean except 2 expected warnings (=mpv=,
  =youtube-dl=/=yt-dlp= missing, both needed only for elfeed-tube's mpv
  follow-along feature)
- [X] User installs =mpv= + =yt-dlp= (agent has no passwordless =sudo=);
  =doom doctor= re-run 2026-08-10 — fully clean, 46 modules, 151 packages,
  zero warnings
- [X] =docs/inventory.org= entry added for =mpv= 0.37.0-1ubuntu4 and
  =yt-dlp= 2024.04.09-1 (both apt, both confirmed on =PATH=)
- [X] Live-verify in the user's own interactive session: =SPC o r= opens
  elfeed, auto-update fires, elfeed-tube transcript/follow-along works —
  user confirmed all three
- [X] Update ADR-012 =:STATUS:= to =live-verified= (with a =:LOGBOOK:=
  entry) once the above is confirmed

* DONE Phase 4 — Org Agenda and Capture basics
CLOSED: [2026-08-23 Sun]
:PROPERTIES:
:EFFORT:   2h
:END:
Promoted from [[file:docs/roadmap.org][docs/roadmap.org]]'s old "Org Agenda and personal project
management" / "Capture and knowledge management" Future-phases bullets,
scoped narrowly per =docs/standards.org= =std-no-scope-creep=. See
[[file:docs/decisions.org][docs/decisions.org]] ADR-017 (agenda/capture basics) and ADR-018 (hand-rolled
Vulpea dailies + backlinks buffer, added mid-scoping once asked for) for the
full decision records.

- [X] =config.el=: =org-agenda-files= (all of =org-directory=, user's
  explicit informed choice), =org-todo-keywords= (matches this file's own
  sequence), =org-capture-templates= ("t" task / "n" note, both into
  =~/org/todo.org='s pre-existing "Inbox" heading; "v" creates a real
  Vulpea-indexed note per Vulpea's own recommended capture pattern)
- [X] =config.el=: Vulpea dailies (=my/vulpea-daily-today/-date/-previous/
  -next=, backed by =vulpea-create= with an explicit =daily/YYYY-MM-DD.org=
  template)
- [X] =config.el=: Vulpea backlinks side buffer (=my/vulpea-buffer-toggle=,
  hand-rolled =org-roam-buffer= equivalent, refreshes on both
  window-selection and window-buffer change — reasoned through the
  single-hook failure mode before writing it, see ADR-018 Evidence)
- [X] New leader bindings under the existing =(n v)= vulpea prefix: "B"
  (backlinks buffer), "j"/"J" (daily today/pick-date), "p"/"n" (daily
  previous/next)
- [X] =docs/inventory.org=: backfilled the =vulpea= package row that was
  missing since its 2026-08-18 install
- [X] =docs/decisions.org=: backfilled ADR-019 (retroactive), recording the
  original Vulpea-vs-org-roam decision that had never itself gotten an ADR
  — explicit about what's genuinely known (the one-line =config.el=
  comment) versus reconstructed from Vulpea's own =docs/comparison.org= (found while scoping this phase)
- [X] =doom doctor= clean after these changes (2026-08-23: 50 modules, 158
  packages, "Everything seems fine"). No =doom sync= needed — this phase
  changed =config.el= only, no =init.el=/=packages.el= edit
- [X] Live-verify in the user's own interactive session: run a capture
  ("t"/"n"/"v"), open the agenda (=SPC n a=), create/visit a daily note,
  toggle the backlinks buffer and confirm it refreshes on both a
  window-switch and an in-window =vulpea-find= jump — all confirmed
  2026-08-23 after an Emacs reload
- [X] Once live-verified, update ADR-017/ADR-018 =:STATUS:= together (both
  now "Implemented, live-verified") with a =:LOGBOOK:= entry each, and move
  this phase's =roadmap.org= status line from "implemented, not yet
  live-verified" to "live-verified" — done 2026-08-23, including the
  per-sub-phase rows in that file's status table

* TODO Phase 5 — Calendar
:PROPERTIES:
:EFFORT:   3h
:END:
Promoted from [[file:docs/roadmap.org][docs/roadmap.org]]'s "Calendar" Future-phases bullet and scoped
2026-08-23 per =docs/standards.org= =std-no-scope-creep=. See
[[file:docs/decisions.org][docs/decisions.org]] ADR-025 for the full decision record. Packages are installed
(the ADR-029 sweep enabled the module); nothing is configured.

- [X] *Do this first:* read org-gcal's actual source — done 2026-08-23. Every
  variable ADR-025 named exists, but three things differ, and ADR-025's
  LOGBOOK now records them: the client id/secret can't be lazy (org-gcal reads
  them literally at load into =oauth2-auto-additional-providers-alist=, so
  they're set as strings plus =org-gcal-reload-client-id-secret=); tokens
  belong to =oauth2-auto= (=oauth2-auto-plstore=), making the module's
  =org-gcal-token-file= vestigial; and =org-gcal-sync= is already
  =deferred=-async, so the hook needed no special async handling
- [X] =init.el=: uncomment =calendar= under =:app=, then =doom sync= — done
  2026-08-23 as part of the ADR-029 module sweep, not as Phase 5 work; the
  module now sits at =init.el:202= and its packages are installed
- [ ] Google Cloud (user action, not agent-doable): add the Calendar scope to
  the existing OAuth client behind the =mail/oauth2-google-client= pass entry,
  re-authorize both accounts. Verify that pass entry actually exists and check
  which *field* holds the client id — the agent still can't see the store
  (=pass= not on its =PATH=, =~/.password-store= not visible), so =config.el=
  tries =client_id=, =client-id=, =login=, =user= in that order and warns
  instead of erroring if none match. Tell the agent the field name if it's
  something else
- [X] =config.el=: =org-gcal-client-id=/=-client-secret= from
  =mail/oauth2-google-client=, and =org-gcal-fetch-file-alist= mapping the two
  accounts to =~/org/calendar/personal.org= and =~/org/calendar/ualberta.org=
  (both directory and files created). Not the lazy ADR-006 pattern the scope
  called for — org-gcal's API rules it out, see above. Both lookups pass
  =+pass-get-field='s NOERROR argument: a missing entry would otherwise signal
  out of the =require= inside =org-gcal-sync= and break opening the agenda.
  =oauth2-auto-plstore= repointed at =doom-state-dir= so the token file
  doesn't land in Doom's own git checkout
- [X] =config.el=: =SPC o C= for manual =org-gcal-sync= (unguarded — an
  explicit request always fetches), plus =SPC o c= for =calendar= (calfw).
  Both keys were free in Doom's =SPC o= map
- [X] =config.el=: agenda-open hook with the staleness guard —
  =my/org-gcal-sync-min-interval= is 3600s (user's choice), tracked against
  =my/org-gcal-last-sync=. No async work was needed: =org-gcal-sync= is
  already =deferred=. Two further guards the scope didn't anticipate: skip
  while =org-gcal--sync-lock= is held (it signals a =user-error= otherwise,
  which from a hook breaks the agenda), and skip entirely until
  =oauth2-auto-plstore= exists, so a hook never starts an authorization flow
- [X] =doom doctor= clean — 77 modules, 230 packages, only the pre-existing
  =markdown +tree-sitter= warning. No =doom sync= needed: =config.el= only
- [ ] Live-verify in the user's own session: calfw renders both calendars, an
  event created in Google appears in org after sync, an org edit pushes back
  to Google, =SPC n a= not noticeably slower
- [ ] Confirm a refresh token survives past the ~7-day mark that bit ADR-011
  (a calendar-week-later check, not same-day)
- [ ] Once live-verified, update ADR-025 =:STATUS:= with a =:LOGBOOK:= entry
  and move the =roadmap.org= Phase 5 status line from "scoped, not
  implemented" to "live-verified"

Standing risk to keep in view: this is the first part of the config that
writes to a remote service rather than to local git-recoverable files. Mostly
recoverable — Google keeps deleted events in Trash ~30 days — but events with
other attendees notify them on every change, and that mail can't be unsent.
=~/org/calendar/= is org-gcal-owned; nothing hand-written goes in there.

* Backlog (untimed, not tied to a phase)

** DONE Migrate claude-code-ide.el's terminal backend from vterm to ghostel
User intends to eventually switch =claude-code-ide-terminal-backend= from
='vterm= to ='ghostel= (an embedded-Ghostty backend claude-code-ide.el
already supports alongside =vterm=/=eat=, see =claude-code-ide.el:226-234=).
Interim fix for the vterm resize-repaint bug that motivated this is the
=window-size-fixed= freeze in =config.el= (2026-08-07, see the
=vterm-mode-hook= comment there).

- [X] Confirm =ghostel= is still maintained and =ghostel-exec= is available
  — Doom ships a =:term ghostel= module (maintained by @hlissner, since
  26.07.0) declaring =ghostel=/=evil-ghostel=, providing =ghostel-exec=;
  confirmed by reading =claude-code-ide.el='s own backend dispatch.
- [X] Package installed (explicit approval given 2026-08-10) — =init.el=:
  =ghostel= added alongside =vterm= in =:term= (not replacing it yet).
  =doom sync= clean, 190 packages. =doom doctor= clean except one expected
  warning (missing =zig=, only needed to compile from source — pre-built
  binaries cover this machine's =x86_64-linux=). See [[file:docs/inventory.org][inventory.org]].
- [X] User runs =M-x ghostel= interactively once to trigger the native
  =libghostty-vt= module's pre-built-binary download
  (=ghostel-module-auto-install=) — needs a real interactive session, not
  agent-doable. Confirmed run 2026-08-17.
- [X] Live-verify ghostel actually renders/resizes correctly before flipping
  the backend over. Confirmed 2026-08-17: resizes fine, rendering correct.
- [X] Set =claude-code-ide-terminal-backend= to ='ghostel= in =config.el=
  (2026-08-17).
- [X] Re-verified the Phase 6 agentic workflow smoke test passes with the
  new backend (2026-08-17) — full re-run against a fresh disposable scratch
  project in an isolated daemon, same seeded-bug task as the original vterm
  test: bug found and fixed, tests run, marker written, no git actions,
  clean teardown. Surfaced and resolved a new gotcha: ghostel needs a real
  X11 frame to render (frame-less headless daemons don't work for it). Full
  writeup: [[id:adr-terminal-backend-ghostel][ADR-014]].
- [X] =my/project-vterm= and Doom's built-in terminal popup (=:term vterm=
  in =init.el=) migrated too (2026-08-17): =vterm= commented out of
  =init.el='s =:term= block entirely (nothing left using it —
  =AGENTS.md='s "one tool per responsibility" rule now holds cleanly);
  =my/project-vterm= renamed to =my/project-ghostel= and rewritten to call
  =(ghostel)=; =SPC o t= (Doom's built-in popup) confirmed resolving to
  =+ghostel/toggle=. =doom sync=/=doom doctor= clean (46 modules, 151
  packages). Full writeup: [[id:adr-terminal-backend-ghostel][ADR-014]]'s LOGBOOK.
- [X] =window-size-fixed= freeze removed outright (2026-08-17) rather than
  ported to ghostel — no equivalent resize corruption observed, and
  claude-code-ide.el already re-syncs ghostel's window size on resize
  itself. Revisit only if the same visual corruption recurs under ghostel
  (see ADR-014's Revisit-when).

Closed 2026-08-23: the user restarted Emacs and confirmed everything working,
so the ghostel backend, the module sweep, and the AI-keymap move are all live
in their session. That same restart also cleared the pending live-verification
on [[file:docs/decisions.org][ADR-026]], ADR-027, ADR-028, and ADR-029 — all four moved from
"Implemented; not live-verified" to live-verified, with the per-ADR caveat that
individual features were not itemized back one by one.

** DONE Emacs as a torrent client — transmission.el + user-session daemon
CLOSED: [2026-08-29 Sat]
:PROPERTIES:
:EFFORT:   1h
:END:

Requested 2026-08-23, off-roadmap (the roadmap's active phase is AI
integration, and this isn't that). Full writeup: [[id:adr-torrent-transmission][ADR-030]].

- [X] Backend chosen: =transmission-daemon= + =transmission.el= over the
  already-installed qBittorrent, because no maintained Emacs client exists
  for the latter and writing the WebUI glue would cost more than the one
  =apt install= — see ADR-030's Candidates table.
- [X] =transmission-daemon= =4.0.5= installed by the user (2026-08-23).
- [X] Packaged system unit diagnosed as broken on this machine
  (=Type=notify= timeout, not a daemon fault) and replaced with a user unit
  at =~/.config/systemd/user/transmission-daemon.service= — see
  [[id:tshoot-transmission-system-unit][troubleshooting.org]].
- [X] RPC hardened: =rpc-bind-address= changed =0.0.0.0= -> =127.0.0.1=.
  Authentication stays off, which is why no credential entry is involved.
- [X] =(package! transmission)= declared; =config.el= gains =SPC o B=
  (=SPC o t=/=T= belong to ghostel), =transmission-refresh-modes=, and a
  =browse-url-handlers= entry routing =magnet:= links to =transmission-add=.
- [X] =doom sync= clean (9.5s); =doom doctor= clean apart from the standing
  markdown tree-sitter warning.
- [X] Emacs-side RPC round trip confirmed by an actual =session-get= call
  returning the user daemon's =config-dir= and =download-dir=.
- [X] Live-verified 2026-08-23 after the restart: =SPC o B= opens the torrent
  list, and a =magnet:= link reaches the daemon instead of a browser. The
  auto-refresh is verified by mechanism only — it polls just while the buffer
  is current, by upstream's design, and there were no torrents to watch tick.
  ADR-030 promoted to =Implemented, live-verified= with that gap recorded in
  its =:LOGBOOK:=.
- [X] Desktop-level =magnet:= handler (2026-08-23): the =browse-url-handlers=
  entry only covers links originating inside Emacs, so magnets clicked in
  Firefox still went to qBittorrent. =~/.local/bin/emacs-magnet= plus
  =~/.local/share/applications/emacs-magnet.desktop= now route them to the
  daemon. Both live outside =$DOOMDIR= and so outside git — ADR-030's
  Consequences and Rollback are their only record.
- [X] Watch the auto-refresh tick during a real transfer, the one check a
  torrent-less daemon could not provide — done 2026-08-29 against a throttled
  Debian =netinst= transfer; the list buffer's columns tick while the buffer is
  current. Firefox also confirmed: its =handlers.json= had no cached
  qBittorrent association to begin with (=magnet= is =action 4=,
  use-system-default), and a magnet clicked in Firefox reached the daemon
  through =xdg-desktop-portal=. Both written up in ADR-030's =:LOGBOOK:=, along
  with an unmitigated disk-exhaustion risk the test surfaced.
- [X] System unit masked and lingering enabled (2026-08-23), both optional
  and neither needed for the client to work: =sudo systemctl mask
  transmission-daemon= (user's own terminal) stops the broken unit failing
  at boot, and =loginctl enable-linger james= (no sudo needed — polkit
  allows it for one's own account) keeps seeding alive after logout. The
  masked unit still appears in =systemctl --failed= until =sudo systemctl
  reset-failed transmission-daemon= or the next reboot.

** DONE EPUB reading in Emacs — nov.el
CLOSED: [2026-08-29 Sat]
:PROPERTIES:
:EFFORT:   30m
:END:

Off-roadmap (the roadmap's active phase is AI integration, and this isn't
that), same shape as the transmission entry above. Full writeup:
[[id:adr-epub-nov][ADR-034]].

- [X] =packages.el=: =(package! nov)=; =config.el=: =use-package! nov= with
  =:mode ("\\.epub\\'" . nov-mode)= and one setting, =(setq nov-text-width t)=
  — =perfect-margin= already owns the text column, so nov's own fill would
  wrap a second time inside those margins.
- [X] Verified nothing else needs configuring: Evil bindings come from
  =evil-collection='s =nov= module (=+everywhere= is on), reading position
  from Doom's global =save-place-mode=, file browsing from dirvish. Sources
  cited in ADR-034's Evidence.
- [X] =doom sync= — =nov= 0.5.0 and its =esxml= dependency are both built.
- [X] =doom doctor= clean after the =packages.el= change (2026-08-29: 77
  modules, 231 packages; the only warning is the standing false "development
  build of Emacs (31.1)" one).
- [X] Live-verify in the user's own session — confirmed 2026-08-29. Reported
  as working overall rather than itemized check by check.
- [X] =docs/inventory.org=: =nov= row added 2026-08-29 (=874daf5= / 0.5.0),
  noting =esxml= =6a37588= / 0.3.8 as the dependency it brought with it — the
  table tracks =packages.el= declarations, so =esxml= gets a mention in that
  row rather than a row of its own.
- [X] Once live-verified, update ADR-034 =:STATUS:= with a =:LOGBOOK:= entry
  — done 2026-08-29, status now =Implemented, live-verified=.

** TODO Literate configuration — config.org tangling Emacs and the shell layer
:PROPERTIES:
:EFFORT:   3h
:END:

Requested 2026-08-29 while scoping a guard against ADR-033's twice-repeated
shell-layer regression. Off-roadmap, same shape as the transmission and EPUB
entries. Full writeup: [[id:adr-literate-config][ADR-035]].

- [X] Verify the design's four load-bearing assumptions before writing
  anything: per-block =:tangle= targets reach outside =$DOOMDIR=; blank-line
  padding behaviour; a column-0 =*)= in =~/.bashrc= survives a =sh= block; and
  the whole conversion round-trips. All four checked, see ADR-035's Evidence.
- [X] Generate =config.org= from the existing sources and verify all six
  tangle targets come back *byte-identical* in an isolated scratch directory
  against a fake =$HOME=.
- [X] Two deliberate additions on top of the verbatim copy: narrow Doom's
  tangle-on-save hook to =config.org= itself, and put a generated-by banner at
  the head of each tangled dotfile.
- [X] Enable =:config literate= in the =init.el= block, tangle live, back up
  the three dotfiles first (=~/.bashrc.bak-20260829-202429= and siblings, plus
  a set outside =$HOME=).
- [X] =doom sync= clean; =doom doctor= clean (78 modules, 231 packages, one
  standing false warning). =check-parens=, =bash -n= and =git config --list=
  all pass on the tangled output.
- [X] Update =AGENTS.md= and =.claude/rules/doom-emacs.md=: edits go to
  =config.org=, never the tangled files.
- [ ] Live-verify in the user's own session: restart Emacs and confirm the
  config loads (especially the =defadvice!=, which has never run); save
  =config.org= and confirm a retangle; save a =docs/*.org= file and confirm
  *no* retangle; open a new terminal and confirm the shell layer still behaves.
- [ ] Once live-verified, update ADR-035 =:STATUS:= with a =:LOGBOOK:= entry.
- [ ] Later, incremental: promote in-code comments to Org prose section by
  section, where it earns its keep. Deliberately not done during the migration
  — it would have made the byte-for-byte check impossible.

** TODO Phase 7 — Capability activation: use what is already installed
:PROPERTIES:
:ID:       project-phase-7
:EFFORT:   8h
:END:

Scoped 2026-08-30 from an audit of enabled-but-unused modules and packages, and
of the Emacs 30.x/31.1 built-ins this config predates. Roadmap entry:
[[id:roadmap-phase-7][Phase 7]]. Decisions: [[id:adr-module-flag-sweep][ADR-036]]
(module flags and =:lang= sweep) and [[id:adr-emacs-31-builtins][ADR-037]] (Emacs
31.1 built-ins).

*** 7.0 External binaries
Done 2026-08-30. A one-off =scripts/install-deps.sh= was written to drive this
(idempotent, =command -v= guarded, with =--check= and =--optional= modes) and
*deleted afterwards at the user's request* — it had served its purpose and a
provisioning script is out of scope until the "reproducible machine
provisioning" future phase wants one properly. The durable part is the list
below, which is why it is recorded here rather than left in a deleted file.

- [X] Installed, by tier:
  - *A, unblocks existing config:* =pass=, =pass-otp=, =pyright= (there were no
    global npm packages at all, so lsp-mode never started for Python, which also
    disabled =my/python-flycheck-chain-lsp-then-ruff=), and the poppler headers
    =libpng-dev zlib1g-dev libpoppler-glib-dev libpoppler-private-dev= so
    =pdf-tools-install= could build =epdfinfo= (its server had only ever existed
    as =.c= sources). =ruff= and =languagetool= installed by the user via snap.
  - *B, the four new doctor warnings:* =libxml2-utils= (=xmllint=), =tidy=,
    =stylelint= + =stylelint-config-standard=, =js-beautify=.
  - *C, language servers:* =yaml-language-server=, =typescript=,
    =typescript-language-server=. Already present: =bash-language-server=,
    =vscode-json/html/css-language-server=, =texlab=, =marksman=.
  - *D, formatters:* =prettier=, =isort=, =mypy=. Already present: =black=,
    =shfmt=, =shellcheck=.
  - *E, LaTeX:* =latexmk=, =texlive-extra-utils= (=latexindent=), =biber=,
    =texlive-xetex=.
- [X] Deliberately *not* installed, with reasons: =emacs-lsp-booster= (Doom's own
  doctor errors "+booster does nothing without +eglot"; this config is
  lsp-mode), =hunspell= (aspell and enchant-2 work), =mpd= (emms drives mpv),
  =go= and =nix= (no module enabled).
- [X] =M-x pdf-tools-install= run — =epdfinfo= now built at
  =straight/build-31.1/pdf-tools/epdfinfo=, so =:tools pdf=, =org-noter=,
  =org-pdftools= and images in =nov= work for the first time.
- [X] =doom doctor= down to *one* warning: the standing false "development build
  of Emacs (31.1)" positive.
- [X] Two bugs the script surfaced, worth keeping: =latexmk= is its own apt
  package on Ubuntu (=texlive-extra-utils= provides =latexindent= but *not*
  =latexmk=), and =pass-otp= ships a pass *extension* at
  =/usr/lib/password-store/extensions/otp.bash= rather than a binary, so
  =command -v pass-otp= never finds it and reports it perpetually missing.

*** 7.1 Repair inert capability
- [X] Install tree-sitter grammars — 19 of them, via Doom's own ABI-gated
  =treesit-language-source-alist=, into
  =~/.config/emacs/.local/etc/tree-sitter/=. Verified end-to-end: =t.py=,
  =t.json= and =t.yaml= now open in =python-ts-mode=, =json-ts-mode= and
  =yaml-ts-mode=; before this they all fell back.
- [X] Correct three stale documentation lines: =docs/standards.org= still
  forbade literate configuration (ADR-035 did it), =docs/architecture.org= still
  said Phase 1 was in progress, and =docs/ai/workflows.org= still named vterm as
  the terminal host (ghostel replaced it 2026-08-17, and the same file's
  =wf-agent= section already said so).
- [ ] =make= and =bibtex= grammars build and install but report unavailable —
  the =.so= exists and its language symbol does not match. Cosmetic; neither has
  a major mode in use here.
- [ ] Re-verify elfeed's =+youtube= path and correct the "inert until mpv is
  installed" comment in =config.org= — =mpv= and =yt-dlp= are both present now.
- [ ] Decide =:checkers grammar=: =languagetool= is installed now, so wire it or
  record =writegood-mode= as the working half.

*** 7.2 Module flags and new =:lang= modules ([[id:adr-module-flag-sweep][ADR-036]])
- [X] Enable =vertico +childframe=, =syntax +childframe +icons=,
  =format +onsave +lsp=, =latex +cdlatex +fold=, =org +present +crypt=,
  =:lang data=, =graphviz=, =rest +jq=, =web +tree-sitter=, =yaml +tree-sitter=.
- [X] Two =doom sync= passes (=init.el= edits land one sync late); new packages
  built: vertico-posframe, flycheck-posframe, cdlatex, org-tree-slide,
  restclient, ob-restclient, web-mode, yaml-mode, graphviz-dot-mode, csv-mode.
- [X] =doom doctor=: five warnings — the standing false "development build"
  positive, plus four absent optional binaries for =:lang data=/=:lang web=,
  all covered by tier B of the install script.
- [X] Declined with reasons recorded in the ADR: =corfu +tng= (rewrites the
  live-verified TAB contract), =org +journal= (Vulpea owns dailies per ADR-027),
  =lsp +booster= (Doom's own doctor: "+booster does nothing without +eglot"),
  =:lang nix= and =:lang plantuml= (no binary), =python +uv= (no binary).
- [ ] Live-verify in the user's own session: both childframes in a real
  graphical frame, =format +onsave +lsp= not fighting the explicit =black=
  formatter, and tree-sitter highlighting in Org and LaTeX specifically.

*** 7.3 Emacs 31.1 built-ins ([[id:adr-emacs-31-builtins][ADR-037]])
- [X] Adopt seven: =trusted-content= (scoped to this repo and =~/org=, not
  =:all=), =kill-region-dwim=, =eldoc-help-at-pt=,
  =split-window-preferred-direction=, =minibuffer-regexp-mode=,
  =find-function-mode=, and hideshow's =hs-show-indicators= /
  =hs-display-lines-hidden=.
- [X] Verify all seven live in a disposable headless daemon, with a clean
  startup log.
- [X] Record twelve rejected candidates with reasons, so the question is not
  re-asked at the next Emacs release. The three worth remembering:
  =completion-preview-mode= is a third engine on the Corfu/Copilot axis;
  =vc-auto-revert-mode= duplicates Doom's own =doom-auto-revert-mode=;
  =mode-line-collapse-minor-modes= is inert because Doom already sets
  =doom-modeline-minor-modes nil=.
- [X] =timeout= withdrawn from the plan on inspection — the Vulpea backlinks
  updater already short-circuits when the panel is hidden and when the note ID
  is unchanged, and no slowness has been observed. Debouncing it would optimize
  a problem nobody has reported.
- [ ] Live-verify: =hs-show-indicators= in the fringe alongside
  =:ui vc-gutter +pretty=, and =find-function-mode='s =C-x= bindings against
  Evil and Doom.

*** 7.4 Wire the unwired packages — mostly dissolved on inspection
The audit that scoped this phase counted "packages installed but never named in
=config.org=" and got 55. That metric turns out to measure the wrong thing:
Doom configures the overwhelming majority of them at *module* level, so absence
from =config.org= means nothing was needed, not that nothing was done.

Checked against module sources, and against a live headless daemon where a hook
could be observed rather than inferred:

| Believed unwired | Actually | Where |
|------------------+----------+-------|
| =gptel-magit= | Already installed by =:tools llm= via =:hook (magit-mode . gptel-magit-install)=. Binds =M-g= in the commit buffer, adds "Generate commit"/"Generate with rationale" to the =magit-commit= transient and "Explain" to =magit-diff= | =modules/tools/llm/config.el=; hook confirmed present on =magit-mode-hook= |
| =ob-gptel= | Already hooked: =ob-gptel-capf= added to =completion-at-point-functions= in org buffers | same file; confirmed on =org-mode-hook= |
| =org-modern=, =org-appear= | Already hooked into =org-mode= by the =+pretty= flag this config already sets | =modules/lang/org/contrib/pretty.el:7,11=; both confirmed live |
| =toc-org= | Already hooked, with =toc-org-hrefify-default= set | =modules/lang/org/config.el:902= |
| =org-download= | Already configured *and bound* at =SPC m a c/p/P= | same file, =:745-747= |
| =ox-pandoc= | Already configured | same file, =:524= |
| =org-noter= | Available; =+noter= is enabled | module |
| embark, embark-consult, marginalia, consult, =consult-dir=, =consult-flycheck=, =consult-yasnippet=, wgrep, vertico-posframe, vertico-multiform | All have their own =use-package!= blocks in the vertico module | =modules/completion/vertico/config.el:106-335= |
| yasnippet, =auto-yasnippet= | Configured | =modules/editor/snippets/config.el:10,135= |
| dape | Configured, and bound under Doom's debugger leader map | =modules/tools/debugger/config.el:6= |
| magit, forge, git-timemachine, git-link | ~35 bindings under =SPC g=, including the full forge find/browse/list submaps | =modules/config/default/+evil-bindings.el:523-570= |

So this sub-phase is not "write configuration for 55 packages". It is one bug,
one genuine gap, and a large transfer into [[*7.5 Capability cheatsheet][7.5]],
which is now clearly the main deliverable of the phase.

- [X] *Bug found and fixed.* =config.org= bound =SPC g s= to =magit-status=. That
  duplicated =SPC g g= (Doom's own status binding) *and*, because =config.el=
  loads after all modules, overwrote Doom's =SPC g s= =>
  =+vc-gutter/stage-hunk=. Measured in a headless daemon before removal:
  =(lookup-key doom-leader-map (kbd "g s"))= and =(kbd "g g")= both returned
  =magit-status=. Same shadowing mechanism as [[id:adr-llm-module-owns-ai-keys][ADR-028]]'s =SPC o l= collision,
  smaller blast radius. Binding deleted; the reasoning is now a comment at the
  call site so it does not get re-added.
- [X] Merged two duplicate =after! projectile= blocks — the second was a superset
  of the first.
- [X] *Custom agenda views written* — five, keyed =d w b u s=, all built and
  rendered in a headless daemon. Deliberately written against this config's own
  keyword sequence (=TODO= / =IN-PROGRESS= / =BLOCKED= / =DONE= / =REJECTED=);
  most published agenda recipes assume =NEXT= and =WAITING=, which do not exist
  here and would have matched nothing while appearing to work. =d= is a
  composite (Today / In progress / Blocked / Inbox), =w= is the week, =b= is an
  unblocking sweep, =u= is the =todo.org= inbox, and =s= lists TODOs with
  neither a scheduled date nor a deadline — items that no ordinary agenda view
  can show at all. No new keybinding: =SPC n a= already runs the dispatcher.
- [X] *Bug in the new views, caught before shipping.* Doom sets
  =org-agenda-start-day= to ="-3d"= globally, so a span-1 view that does not
  override it renders three days ago under the heading "Today" — the daemon
  showed "Today / 27 August" against a system clock reading the 30th. Fixed with
  =(org-agenda-start-day nil)= in the =d= and =w= blocks; re-verified as
  "30 August 2026".
- [X] *Pre-existing bug found while verifying: =org-agenda-files= covered two
  files, not the tree.* =org-agenda-files= was =(list org-directory)=, and Org
  expands a directory entry *non-recursively*. It resolved to exactly
  =~/org/todo.org= and =~/org/doom.org= — 2 of the 68 =.org= files in the tree.
  =work/= (15 files), =personal/= (3), =calendar/= (2), =daily/=, =mtg/= and
  =test/= were invisible to every agenda view, while both =config.org= and
  [[id:adr-org-agenda-capture-basics][ADR-017]] describe the setting as covering
  "the whole pre-existing =org-directory= tree". The =calendar/= case also
  quietly undercut [[id:adr-calendar-org-gcal][ADR-025]], which assumes synced
  Google events reach the agenda — they could not have. Fixed by listing
  =org-directory= plus its immediate subdirectories: =(org-agenda-files)= now
  returns 26 files from 10 entries. Depth-1 and evaluated at load, so a new
  subdirectory needs a reload.
- [ ] ADR-017 needs a LOGBOOK entry recording that its stated agenda-files
  behaviour never matched reality, and ADR-025's live-verification step should
  note that the calendar files were unreachable until this fix.
- [X] =citar= — *declined by the user 2026-08-30, backed out.* It had been
  configured and verified first (=~/org/bib/references.bib= created,
  =citar-bibliography= set, =org-cite-global-bibliography= confirmed to resolve),
  then removed on the user's call: config section deleted from =config.org=, the
  template =.bib= and its directory deleted, re-tangled. The file contained no
  real entries, only comments, so nothing was lost.

  Worth keeping from the exercise: =:tools biblio= needs exactly *one* setting,
  =citar-bibliography=, and it must be set at top level rather than inside an
  =after!= — Doom derives =org-cite-global-bibliography= from it inside
  =(after! oc ...)=, so a later value arrives too late and the global
  bibliography ends up nil. Same ordering hazard as =oauth2-auto-plstore=. If a
  bibliography is ever wanted, that is the whole job.
- [ ] Decide whether any popup rule is actually missing. The config has one
  =set-popup-rule!= (ghostel), but =:ui popup +defaults +all= is enabled and
  handles the general case, so "only one rule" is not itself evidence of a gap.
- [X] =code-review= rejected without needing an ADR of its own: claude-code-ide
  opens Ediff automatically and Magit plus forge cover the rest, so a third
  review surface has no distinct responsibility ([[id:std-no-duplication][std-no-duplication]]).

*** 7.5 Capability cheatsheet
- [X] =docs/cheatsheet.org= written — 279 lines, 106 table rows, with =:ID:=
  properties and =id:= cross-links per =std-org-first=. All links validated
  against defined IDs; zero broken.
- [X] Every binding read out of module source and cited by file and line, the
  standard [[id:ai-keybindings][keybindings.org]] holds itself to. Nothing recalled
  from memory or from upstream docs, both of which drift from what a given Doom
  checkout does. Nineteen citations that were first written approximately
  (="el:~825"=) were then pinned to exact lines by grepping the source, and two
  were wrong on the first pass and corrected (=evil-surround-region= is at
  =evil/config.el:674=, not 676; =evil-lion= at =679-682=, not =681-684=).
- [X] Sections: motion and jumping, windows, editing, search and lookup, git, AI,
  org and notes, evaluate/build/debug, buffers and UI toggles, plus an explicit
  "exists but has no key yet" list so those are not rediscovered from scratch.
- [X] Deliberately *not* a full keymap dump — =SPC h b b= and which-key already
  do that better and do not rot. The file covers the subset worth knowing about
  rather than discovering by accident, and says so.
- [X] A maintenance note at the end: when a line citation stops matching after a
  Doom upgrade, re-read the source rather than patching the number from memory,
  because a guessed correction quietly destroys the property that makes the file
  worth having.

Highlights the audit had missed entirely, now documented: =gs= is a whole
easymotion layer rather than a single avy key; =SPC s t= / =SPC s T= are the
entire interface to the dictionary and thesaurus stack that =:tools lookup
+dictionary +offline= provides; =SPC f h= jumps straight to a heading in
=config.org=, which matters much more now that ADR-035 made it the
configuration; and =gptel-magit='s =M-g= writes a commit message from the staged
diff with no setup at all.

*** 7.6 Closeout
- [ ] =doom sync=, =doom doctor= clean.
- [ ] =docs/inventory.org=: the "none of the 22 has any configuration" line, the
  =gcc= "unused by this project" line (grammars compile with it now), the
  gptel-magit/gptel-quick/ob-gptel row, the new module and package rows, and
  every new binary per =std-binaries= (role, install method, version check,
  upgrade, rollback).
- [ ] Set =:STATUS:= on ADR-036 and ADR-037 once live-verified.

*** 7.7 Rebuild the =pass= credential store
Found 2026-08-30 while auditing external binaries. =:tools pass +auth= is
enabled and =docs/ai/providers.org= records =api/anthropic= and =api/openai= as
"Live — verified", but nothing backing that survives: no =pass= binary, no
=~/.password-store=, =PASSWORD_STORE_DIR= unset, and
=~/.gnupg/private-keys-v1.d/= empty (directory dated 2020-06-06). No backup of a
store exists anywhere under =/home/james=. So =config.org:740= and =:746= call
=+pass-get-secret= against nothing, and gptel's Anthropic and OpenAI backends
are broken — masked in daily use because claude-code-ide authenticates through
the Claude CLI's own login and never touches =pass=.

This is a rebuild from empty, not a recovery. The API keys have to come from the
provider dashboards again; there is nothing to decrypt.

Steps 2–6 are the user's own — a passphrase and three secret values, none of
which may pass through an agent or appear in any command output.

- [X] =sudo apt install -y pass pass-otp= — both present; =pass-otp= installs a
  pass *extension* at =/usr/lib/password-store/extensions/otp.bash=, not a
  binary, which is worth knowing because =command -v pass-otp= will never find
  it — check for the extension file instead.
- [X] =gpg --full-generate-key= — done 2026-08-30. ed25519 =[SC]= primary with a
  cv25519 =[E]= encryption subkey (the one =pass= actually uses), uid
  "James Cook (PsychedPC) <mrniceguyjames@gmail.com>", trust =[ultimate]=.
  Fingerprint =4F6F02436717EE9B823369EAE2F99AE2D3BDF9E1=, short key ID
  =E2F99AE2D3BDF9E1=. Read back with
  =gpg --list-secret-keys --keyid-format=long=; fingerprints are public by
  design, so recording it here is fine.
- [X] =pass init 4F6F02436717EE9B823369EAE2F99AE2D3BDF9E1= — done 2026-08-30.
  =~/.password-store/.gpg-id= holds the fingerprint.
- [ ] =pass insert api/anthropic= — read by =config.org:740=. Prompts without
  echoing and never enters shell history.
- [ ] =pass insert api/openai= — read by =config.org:746=.
- [ ] =pass insert -m mail/oauth2-google-client= — multiline. Line 1 is the
  client *secret*; line 2 is =client_id: <id>.apps.googleusercontent.com=.

  The field name [[id:roadmap-phase-5][Phase 5]] recorded as unconfirmed is now
  settled *from source*, 2026-08-30. Doom's =+pass-get-field=
  (=modules/tools/pass/autoload/pass.el=) is a =defalias= onto
  =auth-source-pass-parse-entry=, which parses an entry as: line 1 is the secret
  (=auth-source-pass--parse-secret=), and every later line is split at its
  *first* colon into a =key: value= pair with both halves =string-trim='d
  (=auth-source-pass--parse-data=, =auth-source-pass.el:219-233=). Lookup is
  =assoc=, so keys are plain strings and the space after the colon is optional.
  =config.org:1320= already tries =client_id=, =client-id=, =login=, =user= in
  order, so any of the four resolves and the first match wins. Use =client_id=.
- [ ] =pass ls= to confirm the structure. Structure only — never print a value.
- [ ] Correct =docs/ai/providers.org=: the =api/anthropic= and =api/openai= rows
  claim "Live — resolves to a 108/164-char string, verified". True when written,
  false now. Rewrite as a dated historical note plus a "rebuilt 2026-08-30" row.
- [ ] Re-verify gptel against both backends once the entries exist.

**** Knock-on effects
- [ ] [[id:roadmap-phase-5][Phase 5]]'s remaining blocker may have grown. It is
  scoped as "add the Calendar scope and re-authorize both accounts", which
  assumed the OAuth client credentials still existed. If
  =mail/oauth2-google-client= is genuinely gone rather than hidden, that becomes
  re-creating the client in the Google Cloud console first. Re-scope ADR-025's
  remaining work once the store is rebuilt and the truth is known.
- [ ] =~/.authinfo.gpg= (169 bytes, dated 2020-11-08) is encrypted to a key that
  no longer exists in the keyring, so it is unreadable. Determine what
  =auth-source= was reading from it — mu4e is the likely consumer — and whether
  anything still depends on it.
