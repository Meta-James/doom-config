#+TITLE: Roadmap
#+AUTHOR: James Cook
#+STARTUP: overview
:PROPERTIES:
:ID:       docs-roadmap
:END:

Phased direction. Only Phase 1 (AI integration) is active work; everything after it
is named so later phases stay compatible with it, not implemented now.

* Phase 1 — AI integration (active)
:PROPERTIES:
:ID:       roadmap-phase-1
:END:

| Sub-phase | Status | Notes |
|-----------+----------+----------------------------------------------------------|
| 0 — Read-only discovery       | DONE | |
| 1 — Architecture proposal     | DONE | See [[id:docs-decisions][decisions.org]] for the decisions locked in |
| 2 — Baseline modernization    | DONE | Doom core upgraded =3e15fb36d= → =04b2956ba=; EXWM removed; =doom-dashboard= module rename. See [[id:adr-doom-core-upgrade][ADR-001]], [[id:adr-remove-exwm][ADR-002]] |
| 3 — Documentation foundation  | DONE | This tree, validated (56 IDs, 0 duplicates, 57 links resolved) |
| 4 — Chat and focused assistance | DONE | gptel reworked (Anthropic default + OpenAI), =SPC a= flattened, verified via headless daemon and confirmed by the user in their own interactive session (streaming, cancellation, popup, Org-buffer UX, network-down). See [[id:adr-gptel-rework][ADR-006]] |
| 5 — Inline completion         | DONE | copilot.el kept, NES enabled, TAB precedence live-verified, and a real suggestion/NES-edit confirmed working by the user in their authenticated session. See [[id:adr-copilot-nes][ADR-007]] |
| 6 — Primary agentic workflow  | DONE | claude-code-ide.el installed, wired, and live-tested (real session, seeded bug found and fixed, no git actions). Remote/TRAMP still needs hardware. See [[id:adr-claude-code-ide-install][ADR-008]] |
| 7 — Review, permissions, safety | DONE | Permission enforcement implemented and live-verified (=.claude/settings.json= deny list blocks commit/push/rebase/reset/clean), see [[id:adr-permission-enforcement][ADR-009]]. Ediff review path confirmed end-to-end in a real interactive session (Magit accuracy, per-hunk accept/reject/edit, diagnostics/tests re-run, rollback) after fixing a daemon-PATH issue, see [[id:tshoot-daemon-path-systemd][troubleshooting.org]]. |
| 8 — Prompts, memory, skills   | DONE | =CLAUDE.md=/=AGENTS.md= finalized with Phase 4-6 doc pointers. Skill policy decided: ADR-scaffold skill built (=/new-adr=), see [[id:adr-skill-policy][ADR-010]]; other patterns deferred as Phase-1-specific. |
| 9 — Validation and handoff    | DONE | Acceptance tests run, final docs pass done, clean diff ready. User confirmed interrupting the agent works; file-reference clicking was found to never have been a real feature (clarified, not a regression — see [[id:tshoot-file-refs-mis-scoped][troubleshooting.org]]). Remaining gaps are all hardware-gated. See [[id:ai-evaluation][evaluation.org]]. |

See [[id:ai-architecture][docs/ai/architecture.org]] for the Phase 1 subsystem design.

* Future phases (named, not implemented)
:PROPERTIES:
:ID:       roadmap-future
:END:

Listed so Phase 1 decisions don't paint these into a corner — none of this is
scheduled or scoped yet.

- Git and code review depth beyond Magit/Ediff
- Org Agenda and personal project management
- Capture and knowledge management
- Documentation and publishing workflows
- Email
- Calendar
- RSS and communications
- Terminal and remote-system workflow depth (beyond what Phase 1 tests for
  TRAMP/SBC targets)
- File and media management
- EXWM (or another Emacs-centred window-management approach) — EXWM package was
  deliberately removed in Phase 1 ([[id:adr-remove-exwm][ADR-002]]); re-declare when this phase starts
- Notifications and desktop integration
- Reproducible machine provisioning
