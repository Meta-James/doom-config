#+TITLE: Inventory — Verified System State
#+AUTHOR: James Cook
#+STARTUP: overview
:PROPERTIES:
:ID:       docs-inventory
:END:

Verified, dated facts about this machine and this configuration. Not aspirational —
if it's here, it was checked, not assumed. Superseded entries are struck through and
dated rather than deleted, so history stays visible.

* Repository
:PROPERTIES:
:ID:       inv-repository
:END:

| Field   | Value                                            |
|---------+---------------------------------------------------|
| Name    | Meta-James/doom-config                           |
| Remote  | git@github.com:Meta-James/doom-config.git (GithubDoom) |
| Branch  | main                                             |
| Verified| 2026-07-20                                       |

* Doom / Emacs / OS
:PROPERTIES:
:ID:       inv-platform
:END:

#+NAME: platform-history
| Field                  | 2026-07-20 (pre-upgrade)        | 2026-07-20 (post-upgrade)                 |
|------------------------+----------------------------------+--------------------------------------------|
| Doom core commit       | =3e15fb36d= (2026-01-07)        | =04b2956ba= (2026-07-17)                  |
| Doom reported version  | 3.0.0-pre                        | 2.2.0 (see [[id:adr-doom-core-upgrade][ADR-001]] note on version string) |
| Doom modules source    | bundled in core repo             | split to =doomemacs/modules=, =3d516aab6= |
| Packages detected      | 147                              | 141 (net: EXWM + exclusive deps removed)  |
| =doom doctor=          | clean                            | clean (after clearing stale =.elc=, see [[id:ai-troubleshooting][troubleshooting]]) |

| Field       | Value                                                          |
|-------------+-----------------------------------------------------------------|
| Emacs       | 31.1, built from source at =/home/james/src/emacs-build/emacs-31.1= and installed to =/usr/local= 2026-08-26 — AOT native-comp, tree-sitter, gnutls, harfbuzz, cairo, rsvg, sqlite3, GTK3-X11 (was 30.2 via snap, confirmed 2026-08-23; 30.1 at the 2026-07-20 baseline). The snap 30.2 is still installed as the rollback path: =sudo rm /usr/local/bin/emacs= falls back to it |
| Doom recommends | Emacs 27.1–30.2, 30.2 recommended (per doomemacs/doomemacs README, checked 2026-07-20) — 31.1 is *past* that stated range, so upstream Doom issues are the first place to look for anything newly odd |
| Linux       | Ubuntu 24.04.4 LTS (noble), kernel 6.8.0-136-generic             |

* Enabled Doom modules (=init.el=)
:PROPERTIES:
:ID:       inv-modules
:END:

77 modules as of 2026-08-23 (=doom doctor= count; was 43 at the 2026-07-20
baseline, and 55 earlier the same day before the non-language module sweep,
[[id:adr-module-sweep-non-lang][ADR-029]]). Growth since: =:app rss= ([[id:adr-rss-elfeed][ADR-012]]), =:term ghostel= (2026-08-10)
minus =:term vterm= (2026-08-17, [[id:adr-terminal-backend-ghostel][ADR-014]]), the =:ui= rice modules
([[id:adr-ui-rice-pass][ADR-022]]), and the =:email=/=:app= modules from Phases 2-3. Full source of
truth is =init.el= itself — this is a summary, not a duplicate to keep in
sync by hand. Flags below reflect the 2026-08-23 flag audit ([[id:adr-init-flag-audit][ADR-026]]);
=init.el= now annotates every *available* flag per module, not just the
enabled ones.

- =:completion= corfu +orderless +icons +dabbrev, vertico +icons (Company →
  Corfu 2026-08-18, see [[id:adr-corfu-migration][ADR-021]]; =+dabbrev=/=+icons= added 2026-08-23)
- =:ui= doom, dashboard, doom-quit, emoji +ascii +github +unicode, hl-todo,
  indent-guides, ligatures +extra, minimap, modeline, ophints, popup
  +defaults +all, smooth-scroll +interpolate, tabs, treemacs +lsp, unicode,
  vc-gutter +pretty, vi-tilde-fringe, window-select +numbers, workspaces,
  zen +focus
  (indent-guides/minimap/tabs/zen added 2026-08-21, see [[id:adr-ui-rice-pass][ADR-022]];
  smooth-scroll 2026-08-23)

*Phase 7 flag and module additions, 2026-08-30 ([[id:adr-module-flag-sweep][ADR-036]]):* =:completion vertico
+childframe=; =:checkers syntax +childframe +icons=; =:editor format +lsp=;
=:lang latex +cdlatex +fold=; =:lang org +present +crypt=; and five language
modules enabled — =data=, =graphviz=, =rest +jq=, =web +tree-sitter=, =yaml
+tree-sitter=. Packages that came with them: vertico-posframe,
flycheck-posframe, cdlatex, org-tree-slide, centered-window, org-re-reveal,
restclient, ob-restclient, web-mode, yaml-mode, graphviz-dot-mode, csv-mode.

Declined in the same sweep, with reasons recorded in ADR-036 so they are not
re-proposed: =corfu +tng= (rewrites the live-verified TAB precedence contract),
=org +journal= (Vulpea owns dailies, [[id:adr-org-roam-with-vulpea][ADR-027]]), =lsp +booster= (needs
=+eglot=), =:lang nix= and =:lang plantuml= (no binary), =python +uv= (no
binary), and =yaml +lsp= / =web +lsp= (servers were absent at the time; they are
installed now, so these two are cheap to add).

*Tree-sitter grammars, 2026-08-30:* 19 installed into
=~/.config/emacs/.local/etc/tree-sitter/= — python, json, bash, markdown,
markdown-inline, yaml, toml, c, regex, latex, org, html, css, javascript,
typescript, tsx, jsdoc, plus make and bibtex which build but report unavailable
(symbol-name mismatch; neither has a major mode in use here). Before this,
=treesit-language-available-p= returned nil for *every* language while three
modules carried =+tree-sitter= flags, so every =*-ts-mode= silently fell back.
- =:editor= evil +everywhere, file-templates, fold, format +onsave *+lsp*, lispy,
  multiple-cursors, rotate-text, snippets, whitespace +guess +trim, word-wrap
- =:emacs= dired +icons +dirvish, electric, eww, ibuffer +icons, tramp, undo,
  vc
- =:term= ghostel +everywhere (sole terminal backend since 2026-08-17,
  [[id:adr-terminal-backend-ghostel][ADR-014]]; =vterm= commented out in =init.el=, not deleted, for easy
  rollback)
- =:checkers= syntax *+childframe +icons*, spell +flyspell +aspell +everywhere,
  grammar (=syntax= had run with *no flags at all* until 2026-08-30;
  =languagetool= for the grammar module's langtool half was installed the same
  day, but langtool itself is still unwired — =writegood-mode= is the working
  half)
- =:tools= biblio, collab +tunnel, debugger, direnv, editorconfig, eval
  +overlay, llm, lookup +dictionary +docsets +offline, lsp +peek, magit
  +forge, make, pdf, tmux, tree-sitter, upload (=llm= added 2026-08-23; it owns
  =gptel= outright — package pin, =gptel-default-mode=, popup rule, and the
  =SPC o l= keymap — and also brings =gptel-magit=, =gptel-quick=, =ob-gptel=.
  This repo's duplicate declarations, its =SPC a= namespace, and the =SPC o l=
  app launcher that had been shadowing the module's keymap were all resolved
  2026-08-23; see [[id:adr-llm-module-owns-ai-keys][ADR-028]])
- =:lang= emacs-lisp, json +lsp +tree-sitter, latex +lsp, markdown +lsp +grip
  +tree-sitter, org +pandoc +roam +gnuplot +jupyter +pretty +noter
  +dragndrop (org-roam alongside Vulpea, Vulpea preferred on every overlap —
  see [[id:adr-org-roam-with-vulpea][ADR-027]]), python +lsp +pyenv +poetry +pyright +tree-sitter, sh +lsp
- =:email= mu4e +org +gmail +mbsync
- =:os= macos (macOS only), tty +osc
- =:app= calendar (org-gcal configured, not yet authorized against Google —
  [[id:adr-calendar-org-gcal][ADR-025]]), emms,
  everywhere, irc, rss +org +youtube
- =:config= default +bindings +smartparens +gnupg

Five modules that no longer exist upstream (=:ui hydra=, =:tools prodigy=,
=:tools rgb=, =:tools taskrunner=, =:app twitter=) were deleted from
=init.el= on 2026-08-23; ten that do exist but were absent were added the
same day — five enabled at the user's request (=:emacs eww=, =:emacs tramp=,
=:editor whitespace=, =:ui smooth-scroll=, =:tools llm=) and five left
commented out as language modules with no current use (=:lang= =ada=,
=graphviz=, =janet=, =odin=, =scad=). See [[id:adr-init-flag-audit][ADR-026]].

The 2026-08-23 non-language sweep ([[id:adr-module-sweep-non-lang][ADR-029]]) then enabled 22 more modules —
the =:ui=, =:editor=, =:emacs=, =:tools=, =:os= and =:app= additions marked
above — at the user's per-module choice. Offered and declined: =:tools
docker=, =:tools ein=, =:tools terraform=, =:tools ansible=, =:term eshell=,
=:ui deft=. Two prerequisite binaries came with it: =tuntox= (for =collab
+tunnel=) and =xdotool= (for =:app everywhere=). None of the 22 had any
configuration in =config.el=; they ran at Doom defaults.

*Reread that sentence carefully as of 2026-08-30 ([[id:adr-module-flag-sweep][ADR-036]]).* It is true and it was
misleading, and the Phase 7 audit initially drew the wrong conclusion from it —
that a large amount of capability sat unwired. It did not. Doom configures most
packages at *module* level, so "no configuration in =config.el=" usually means
none was needed. Verified examples: =gptel-magit= and =ob-gptel= are hooked by
=:tools llm=; =org-modern=, =org-appear=, =toc-org=, =ox-pandoc= and
=org-download= (bound at =SPC m a c/p/P=) by =:lang org=; embark, marginalia,
consult and its extras, wgrep and vertico-posframe by =:completion vertico=;
yasnippet and dape by their own modules; and Magit/forge have ~35 bindings under
=SPC g=. Before writing configuration for anything listed as "unconfigured"
here, grep =~/.config/emacs/sources/doom+/modules/= first. Where a module owns
it, the deliverable is [[id:docs-cheatsheet][the cheatsheet]], not elisp.

* Homedir state that has gone missing
:PROPERTIES:
:ID:       inv-homedir-state-loss
:END:

Recorded 2026-08-30. Two separate audits that day — external binaries for
[[id:adr-module-flag-sweep][ADR-036]], then this file's own binary table — found that a
significant amount of configuration living *only* in =$HOME=, outside any
repository, is simply gone. Nothing in this project deleted it and nothing
detected it; both discoveries were accidental, while chasing something else.

*Missing, confirmed by direct check:*

| Thing | Was | Consequence |
|-------+-----+-------------|
| =~/.password-store= | The whole credential store ([[id:adr-gptel-rework][ADR-006]], [[id:adr-email-mu4e][ADR-011]], [[id:adr-calendar-org-gcal][ADR-025]]) | =+pass-get-secret= for =api/anthropic= and =api/openai= failed, so gptel's Anthropic and OpenAI backends were broken. Rebuilt from empty on 2026-08-30 — new GPG key, =pass init= done, entries not yet re-entered |
| =~/.gnupg/private-keys-v1.d/= | The user's GPG secret key | Empty, directory dated 2020-06-06. Also makes =~/.authinfo.gpg= (169 bytes, 2020-11-08) permanently unreadable |
| =msmtp= + =~/.msmtprc= | SMTP send backend ([[id:adr-email-mu4e][ADR-011]]) | mu4e cannot send mail |
| =oama= + =~/.config/oama/= + =~/.local/state/oama/= | Gmail XOAUTH2 token manager ([[id:adr-email-mu4e][ADR-011]]) | No token refresh, so mbsync cannot authenticate either |
| =~/.mbsyncrc= | IMAP sync configuration | The =mbsync= binary survives with nothing to run |
| =mu= index | =~/.cache/mu=, =~/.local/share/mu= | mu4e has no index |
| =~/.mail/ualberta*= | One of the two configured accounts | 498M of mail survives under =~/.mail=, but only =mrniceguyjames-gmail= and =archive= |

*Survived:* =~/.bashrc=, =~/.inputrc=, =~/.gitconfig= — which is the whole
point. Those three are tangled from =config.org= and under version control
since [[id:adr-literate-config][ADR-035]], written precisely because that layer had already
silently reverted twice. The layers that vanished are exactly the ones still
living only in =$HOME=.

That is the pattern worth acting on, and it generalises past this incident:
=$HOME= state that no repository tracks and no check verifies disappears without
anyone noticing. ADR-035 solved it for three dotfiles by making them tangle
targets. Nothing equivalent covers the mail configuration, and nothing can cover
the =pass= store or the GPG key, which must not be committed — those need a
different guard (an encrypted backup, or a startup check that warns when an
expected entry is unreadable), not version control.

*Deliberately not acted on here.* Restoring the mail layer is Phase 2 work and
re-entering credentials is the user's alone. This section exists so the next
session starts from measured fact rather than from this file's previously
confident "yes" rows.

* Explicit packages (=packages.el=)
:PROPERTIES:
:ID:       inv-packages
:END:

| Package        | Recipe                                              | Pinned commit (2026-07-20, post-upgrade) |
|----------------+------------------------------------------------------+--------------------------------------------|
| gptel          | declared by =:tools llm=, not =packages.el=          | =ebf0f3d= / header version 0.9.9.5 (module pin, since 2026-08-23) |
| gptel-magit / gptel-quick / ob-gptel | =:tools llm=                  | Module pins; no config of ours — *and none needed*, corrected 2026-08-30. The module hooks all three itself (=modules/tools/llm/config.el=): =gptel-magit-install= on =magit-mode-hook=, which binds =M-g= in the commit buffer and adds "Generate commit" to the =magit-commit= transient; =ob-gptel-capf= into =completion-at-point-functions= in Org buffers. =gptel-quick= is on =SPC o l e=. All three are live |
| copilot        | =(:host github :repo "copilot-emacs/copilot.el" :files ("*.el"))= | =277ca35= / tagged release 0.9.0 |
| perfect-margin | MELPA default                                       | unrelated to AI work, untouched            |
| claude-code-ide | =(:host github :repo "manzaltu/claude-code-ide.el")= | Installed Phase 6, 2026-07-20; no tagged release upstream yet (pre-1.0) |
| vulpea         | MELPA default                                       | =2ea4a8b= (2026-08-17), installed 2026-08-18; see [[id:adr-vulpea-note-management][ADR-019]] (retroactive) |
| transmission   | MELPA default                                       | installed 2026-08-23; front-end for a local =transmission-daemon=, see [[id:adr-torrent-transmission][ADR-030]] |
| nov            | MELPA default                                       | =874daf5= (2025-12-12) / header version 0.5.0, installed 2026-08-29; EPUB reader, see [[id:adr-epub-nov][ADR-034]]. Pulls in =esxml= =6a37588= (2026-03-29) / 0.3.8 as its only dependency — nothing else installed here declares =esxml=, so it arrived with =nov= |
| ~~exwm~~       | removed 2026-07-20, see [[id:adr-remove-exwm][ADR-002]]         | —                                          |

* External binaries
:PROPERTIES:
:ID:       inv-binaries
:END:

| Binary  | Present | Path / version                          |
|---------+---------+-------------------------------------------|
| rg      | yes     | /usr/bin/rg                                |
| fd      | yes     | apt =fd-find= =9.0.0-1=, which installs the binary as =/usr/bin/fdfind=. Predates this project. The =~/.local/bin/fd= symlink this row used to describe is gone (see [[id:adr-terminal-layer-pass][ADR-033]]'s LOGBOOK — the whole shell-half regression that took it out); =FZF_DEFAULT_COMMAND= now calls =fdfind= directly, so nothing here depends on that symlink existing any more |
| git     | yes     | /usr/bin/git                               |
| node    | yes     | =~/.nvm/versions/node/v24.19.0/bin/node=, =v24.19.0=. *Corrected 2026-08-30* — this row said "v18.19.1, apt package =nodejs=, no nvm/fnm/volta", which is now wrong on every count: node is nvm-managed and three majors newer. npm's global prefix is therefore the nvm tree, not =/usr/local=, so =npm install -g= no longer needs the =--prefix ~/.local= workaround the lifecycle table below still describes |
| python3 | yes     | pyenv shim                                 |
| pyright | yes     | =~/.nvm/versions/node/v24.19.0/bin/pyright=, =1.1.413=. *Corrected 2026-08-30* — this row claimed =/usr/local/bin/pyright=, but pyright was **not installed at all**: there were no global npm packages on the machine. So =:lang python +pyright= had no server, lsp-mode never started for Python, and =my/python-flycheck-chain-lsp-then-ruff= (which requires a live =lsp-mode=) never ran either. Installed during Phase 7 |
| ruff    | yes     | =/snap/bin/ruff=, =0.16.4=. *Corrected 2026-08-30* — this row claimed =~/.local/bin/ruff=; ruff was missing entirely, making the ruff flycheck checker, the LSP-then-ruff chain and =my/python-ruff-fix-buffer= (~40 lines of =config.org=) dead code. Installed by the user via snap during Phase 7 |
| black   | yes     | =/usr/bin/black= (apt, not =~/.local/bin= as this row previously said) |
| poetry  | yes     | ~/.local/bin/poetry                        |
| pyenv   | yes     | ~/.pyenv/bin/pyenv                         |
| direnv  | yes     | =/usr/bin/direnv=, =2.32.1-2ubuntu0.24.04.3= (apt) — predates this project; wired into Emacs via =:tools direnv=/envrc and into bash via a =~/.bashrc= hook, [[id:adr-everywhere-direnv][ADR-032]] |
| pass    | yes     | /usr/bin/pass                              |
| gpg     | yes     | /usr/bin/gpg, one secret key (created 2026-02-05) |
| claude  | yes     | ~/.local/bin/claude, CLI version 2.1.216 (updated from 2.1.215 mid-project) |
| cmake, make, gcc | yes | =build-essential= 12.10ubuntu1, =cmake= 3.28.3-1build7 — needed once, to build vterm's native module (see below) |
| codex   | no      | not installed                              |
| aider   | no      | not installed                              |
| gh      | no      | not installed                              |
| mu / mu4e | yes   | =/usr/bin/mu=, =1.10.8= (apt =maildir-utils=/=mu4e=) — Phase 2, [[id:adr-email-mu4e][ADR-011]] |
| mbsync (isync) | binary yes, config no | =/usr/bin/mbsync= still present, but =~/.mbsyncrc= is **gone as of 2026-08-30** — the binary has nothing to run. See [[id:inv-homedir-state-loss][the homedir-state note]] |
| msmtp   | *no*    | *Was* =/usr/bin/msmtp= =1.8.24=. **Gone as of 2026-08-30**, along with =~/.msmtprc=. mu4e cannot send. See [[id:inv-homedir-state-loss][the homedir-state note]] |
| oama    | *no*    | *Was* =~/.local/bin/oama= =0.22.0=. **Gone as of 2026-08-30**, along with =~/.config/oama/= and =~/.local/state/oama/=. The Gmail XOAUTH2 refresh cycle has no token manager, so mbsync cannot authenticate either. See [[id:inv-homedir-state-loss][the homedir-state note]] |
| pass    | yes     | =/usr/bin/pass= — installed 2026-08-30 (Phase 7). The *store* is separate: it did not exist at all until the same day, see [[id:inv-homedir-state-loss][the homedir-state note]] |
| pass-otp | yes    | apt =pass-otp= =1.2.0-10=. Installs a pass *extension* at =/usr/lib/password-store/extensions/otp.bash=, **not** a binary — =command -v pass-otp= will always report it missing |
| xmllint | yes     | =/usr/bin/xmllint= (apt =libxml2-utils=) — XML formatting for =:lang data= (2026-08-30) |
| tidy    | yes     | =/usr/bin/tidy= — HTML formatting for =:lang web= (2026-08-30) |
| stylelint / js-beautify / prettier | yes | npm globals in the nvm tree — CSS linting and JS/CSS/HTML/markdown formatting for =:lang web= (2026-08-30) |
| yaml-language-server / typescript-language-server | yes | npm globals — prerequisites for adding =+lsp= to =:lang yaml= / =:lang web=, which is *not* yet enabled |
| isort / mypy | yes | pipx — optional Python extras (2026-08-30) |
| latexmk | yes     | =/usr/bin/latexmk= (apt =latexmk=, **its own package** — =texlive-extra-utils= provides =latexindent= but not this). AUCTeX's default build command; =auctex-cont-latexmk= was installed and unusable without it |
| latexindent / biber / xelatex | yes | apt =texlive-extra-utils=, =biber=, =texlive-xetex= (2026-08-30) — LaTeX formatting, biblatex bibliographies, unicode documents |
| epdfinfo | yes    | =~/.config/emacs/.local/straight/build-31.1/pdf-tools/epdfinfo=, built 2026-08-30 via =M-x pdf-tools-install= after installing =libpng-dev zlib1g-dev libpoppler-glib-dev libpoppler-private-dev=. **Had never been built** — the server existed only as =.c= sources, so =:tools pdf=, =org-noter=, =org-pdftools=, =saveplace-pdf-view= and images in =nov= had never worked |
| emacs-lsp-booster | *no, deliberately* | Not installed and should not be: Doom's own module doctor errors =+booster does nothing without +eglot= (=modules/tools/lsp/doctor.el:14=) and the lsp-mode half of the integration is commented out behind a TODO (=+lsp.el:151-168=). This config is lsp-mode. Revisit only alongside a real eglot migration |
| mpv     | yes     | =/usr/bin/mpv=, =0.37.0-1ubuntu4= (apt) — Phase 3, [[id:adr-rss-elfeed][ADR-012]] |
| yt-dlp  | yes     | =/usr/bin/yt-dlp=, =2024.04.09-1= (apt) — Phase 3, [[id:adr-rss-elfeed][ADR-012]] |
| xdotool | yes     | =/usr/bin/xdotool=, =3.20160805.1= (apt) — predates this project; also the paste/refocus backend for =emacs-everywhere= on X11 ([[id:adr-everywhere-direnv][ADR-032]]). The agent's own =which= check first reported it missing; user confirmed it was installed the whole time — see [[id:adr-app-launcher][ADR-020]]'s LOGBOOK and [[id:tshoot-agent-shell-system-state][troubleshooting.org]] |
| xclip   | yes     | =/usr/bin/xclip=, =0.13-3= (apt) — predates this project; clipboard backend for =emacs-everywhere= on X11 ([[id:adr-everywhere-direnv][ADR-032]]) |
| libpoppler-glib-dev | yes | needed for =pdf-tools='s =epdfinfo= helper to compile. The agent's own =dpkg -s= check first reported it missing; user confirmed it was installed the whole time — see [[id:adr-app-launcher][ADR-020]]'s LOGBOOK and [[id:tshoot-agent-shell-system-state][troubleshooting.org]] |
| grip    | yes     | =~/.local/bin/grip=, =4.6.2= (pipx) — backs =:lang markdown +grip= live preview ([[id:adr-init-flag-audit][ADR-026]]) |
| jupyter | yes     | =~/.local/bin/jupyter=, jupyterlab =4.6.3= + ipykernel =7.3.0= (pipx, =--include-deps= to expose the =jupyter= entry point) — backs =:lang org +jupyter= ([[id:adr-init-flag-audit][ADR-026]]) |
| texlab  | yes     | =~/.local/bin/texlab=, =5.26.0=, upstream release binary — LaTeX language server for =:lang latex +lsp= ([[id:adr-init-flag-audit][ADR-026]]) |
| marksman | yes    | =~/.local/bin/marksman=, =2026-02-08=, upstream release binary — Markdown language server for =:lang markdown +lsp= ([[id:adr-init-flag-audit][ADR-026]]) |
| bash-language-server | yes | =~/.local/bin/bash-language-server=, =5.6.0= (=npm -g --prefix ~/.local=) — for =:lang sh +lsp=. npm warned =EBADENGINE= against node 18.19.1; the server runs anyway ([[id:adr-init-flag-audit][ADR-026]]) |
| vscode-json-language-server | yes | =~/.local/bin/vscode-json-language-server= (=vscode-langservers-extracted=, =npm -g --prefix ~/.local=) — for =:lang json +lsp=. Same =EBADENGINE= caveat; starts correctly when given =--stdio= ([[id:adr-init-flag-audit][ADR-026]]) |
| gnuplot | yes     | =/usr/bin/gnuplot=, =6.0 patchlevel 0= (apt, installed by the user 2026-08-23) — backs =:lang org +gnuplot= |
| dot (graphviz) | yes | =/usr/bin/dot=, graphviz =2.43.0= (apt, 2026-08-23) — org-roam graph visualization ([[id:adr-org-roam-with-vulpea][ADR-027]]) |
| sqlite3 (CLI) | yes | =/usr/bin/sqlite3=, =3.45.1= (apt, 2026-08-23) — needed by =:tools lookup +docsets= (dash-docs). Distinct from Emacs' *built-in* sqlite, which is what =magit +forge=, org-roam, and vulpea use |
| wn (wordnet) | yes | =/usr/bin/wn= (apt =wordnet=, 2026-08-23) — offline thesaurus/definitions for =:tools lookup +offline= (wordnut/synosaurus). =doom doctor= does not check for it |
| maim    | yes     | =/usr/bin/maim=, =v5.7.4= (apt, 2026-08-23) — screenshot capture for =org-download-clipboard= (=:lang org +dragndrop=); =xclip= (already present) covers the clipboard half on X11 |
| transmission-daemon / transmission-remote | yes | =/usr/bin/transmission-daemon=, =4.0.5 (a6fe2a64aa)= (apt, installed by the user 2026-08-23) — RPC backend for =transmission.el= ([[id:adr-torrent-transmission][ADR-030]]). Runs as a *user* systemd unit, not the packaged system unit, which fails =Type=notify= startup on this machine — see [[id:tshoot-transmission-system-unit][troubleshooting.org]] |
| qbittorrent | yes  | =qbittorrent 4.6.3-1build2= (apt) — predates this project, GUI only, *not* wired into Emacs. Kept installed; see [[id:adr-torrent-transmission][ADR-030]]'s Candidates for why it wasn't the Emacs backend |
| eza     | yes     | =/usr/bin/eza=, =0.18.2-1= (apt, installed by the user 2026-08-23) — backs the =ll=/=la=/=lt= aliases in =~/.bashrc=; plain =ls= deliberately left alone ([[id:adr-terminal-layer-pass][ADR-033]]) |
| bat     | yes     | =/usr/bin/batcat=, =0.24.0-1build1= (apt, 2026-08-23). Debian/Ubuntu ship the binary as =batcat= — the =bat= name collides with =bacula-console-qt= — so =~/.bashrc= aliases it back. Runs with =BAT_THEME=ansi= so highlighting follows the terminal palette rather than hardcoded hexes ([[id:adr-terminal-layer-pass][ADR-033]]) |
| zoxide  | yes     | =/usr/bin/zoxide=, =0.9.3-1= (apt, 2026-08-23) — =z=/=zi= directory jumping; initialised last in =~/.bashrc= because it wraps =cd= ([[id:adr-terminal-layer-pass][ADR-033]]) |
| fzf     | yes     | =/usr/bin/fzf=, =0.44.1-1ubuntu0.3= (apt, 2026-08-23). Too old for =fzf --bash= (needs 0.48+), so =~/.bashrc= sources =/usr/share/doc/fzf/examples/key-bindings.bash= instead — Ubuntu's package ships no separate bash completion file; that half is automatic via =bash-completion= (=/usr/share/bash-completion/completions/fzf=), already sourced earlier in =~/.bashrc=. Keeps =C-t=/=M-c=; =C-r= is taken by atuin ([[id:adr-terminal-layer-pass][ADR-033]]) |
| delta   | yes     | =/usr/bin/delta=, apt =git-delta= =0.16.5-5= (2026-08-23) — =core.pager= and =interactive.diffFilter= in =~/.gitconfig=, with =syntax-theme=ansi=. CLI git only; magit is unaffected ([[id:adr-terminal-layer-pass][ADR-033]]) |
| atuin   | yes     | =~/.local/bin/atuin=, =18.20.1= (upgraded from =18.19.0= during the 2026-08-27 shell-half restore, see [[id:adr-terminal-layer-pass][ADR-033]]'s LOGBOOK), upstream =x86_64-unknown-linux-musl= release binary, SHA256-verified against the published checksum. *Not* apt (no package) and *not* cargo — atuin 18.19's MSRV is 1.97.0 against this box's rustc 1.75.0. Shell history search on =C-r=; local-only, no =register=/=login= ([[id:adr-terminal-layer-pass][ADR-033]]) |
| rustc / cargo | yes | =/usr/bin/rustc=, =/usr/bin/cargo=, =1.75.0+dfsg0ubuntu1-0ubuntu7.4= (apt, installed by the user 2026-08-23). Nothing in this config builds from source with it; recorded because its version is what ruled out =cargo install atuin= ([[id:adr-terminal-layer-pass][ADR-033]]) |

Node 18.19.1 initially looked like a possible blocker for
=copilot-language-server=. Resolved in Phase 5: this install actually uses the
*npm* path (=copilot-install-server= prefers npm when found on =PATH=, and
=npm= is present) — confirmed by inspecting the already-running server process
(=ps=/=/proc=, read-only, on the user's own long-running daemon): a =node=-run
JS dispatcher execs the platform-native binary as a child process for the
actual LSP work. The installed package (=@github/copilot-language-server=
1.452.0) declares no =engines= constraint in its =package.json= — the
Node-version concern doesn't apply to this package as installed, and the
server has been running successfully for hours as empirical confirmation. See
[[id:adr-copilot-nes][docs/decisions.org ADR-007]].

** Install / upgrade / removal, for binaries this project newly relies on
:PROPERTIES:
:ID:       inv-binaries-lifecycle
:END:

Per [[id:std-binaries][standards.org]] =std-binaries=. Scoped to the binaries Phase 1 actually
introduced reliance on or newly required — =rg=/=git=/=pyright=/=ruff=/
=black=/=poetry=/=pyenv=/=direnv=/=pass=/=gpg= all predate this project and
are pre-existing parts of this machine's Python/dev toolchain, out of scope
for full lifecycle documentation here (per [[id:std-no-scope-creep][std-no-scope-creep]] — Phase 1 is
the AI subsystem, not a full machine audit).

| Binary                      | Role                                    | Install                                                                | Upgrade                        | Removal                                        |
|------------------------------+------------------------------------------+--------------------------------------------------------------------------+----------------------------------+---------------------------------------------------|
| =claude= (Claude Code CLI)  | Backs claude-code-ide.el's agentic session ([[id:adr-primary-agent][ADR-004]]) | Official native installer (=curl -fsSL https://claude.ai/install.sh \vert bash=); lands in =~/.local/share/claude/versions/<ver>=, symlinked from =~/.local/bin/claude= | Self-updates in place (=claude update=, or automatic) — confirmed two versions present on disk (2.1.215, 2.1.216), so an update already happened mid-project without manual reinstall | Delete =~/.local/share/claude= and the =~/.local/bin/claude= symlink; remove the =claude-code-ide= package declaration from =packages.el= and =doom sync= |
| =@github/copilot-language-server= (via =node=/=npm=) | LSP-protocol backend for =copilot.el='s suggestions and NES ([[id:adr-copilot-nes][ADR-007]]) | =M-x copilot-install-server= (fully in-Emacs, reproducible — reads source: uses npm when found on =PATH=, else downloads precompiled native binaries directly, no manual shell step either way) | =M-x copilot-install-server= again re-installs current/latest | Delete =copilot-install-dir= (Copilot's own package-managed install dir); remove =copilot= from =packages.el= and =doom sync= |
| =cmake=, =make=, =gcc= (=build-essential=) | Build toolchain that compiled vterm's native module (=vterm-module.so=) — discovered as a genuine missing prerequisite during Phase 6, see [[id:tshoot-vterm-module][troubleshooting.org]]. =vterm= itself is disabled since 2026-08-17 ([[id:adr-terminal-backend-ghostel][ADR-014]]), but this toolchain is *no longer unused* — corrected 2026-08-30: =gcc= compiles every tree-sitter grammar. Emacs builds them itself via =treesit-install-language-grammar=, and 19 were installed during [[id:adr-module-flag-sweep][ADR-036]]. Removing the toolchain would break grammar installation for any new language | =sudo apt install build-essential cmake= (already present on this machine when checked; not installed by this project) | Standard =apt= upgrade | Standard =apt remove=; unrelated to whether vterm itself stays installed |
| =mu=/=mu4e= | mu4e's indexer + the mu4e elisp package itself (mu4e is *not* straight-managed — deliberately, per the module's own source comment, since it's "prone to breaking updates" and bundled with =mu=) — [[id:adr-email-mu4e][ADR-011]] | =sudo apt install maildir-utils mu4e= — done 2026-07-29 (user's own terminal, sudo denied to the agent by auto-mode's classifier) | Standard =apt= upgrade | Standard =apt remove=; also remove =(mu4e ...)= from =init.el='s =:email= block and =doom sync= |
| =isync= (=mbsync=) | IMAP sync backend for all 3 mail accounts — [[id:adr-email-mu4e][ADR-011]] | =sudo apt install isync= — done 2026-07-29 | Standard =apt= upgrade | Standard =apt remove=; delete =~/.mbsyncrc= |
| =msmtp= | SMTP send backend, recommended by the mu4e module over raw =smtpmail= for multi-account setups — [[id:adr-email-mu4e][ADR-011]] | =sudo apt install msmtp= — done 2026-07-29 | Standard =apt= upgrade | Standard =apt remove=; delete =~/.msmtprc= |
| =oama= | OAuth2 access-token manager for the 2 Gmail accounts' XOAUTH2/OAUTHBEARER refresh cycle, called from =.mbsyncrc=/=.msmtprc= — [[id:adr-email-mu4e][ADR-011]] | Downloaded =oama-0.22.0-Linux-x86_64.tar.gz= from =github.com/pdobsan/oama/releases= (no apt package — this repo's first externally-sourced static binary), extracted =oama= to =~/.local/bin= | Re-download a newer release tarball and overwrite =~/.local/bin/oama= — no auto-update mechanism | Delete =~/.local/bin/oama= and =~/.config/oama/=, =~/.local/state/oama/= |
| =mpv= | Video player backing elfeed-tube's live-transcript/follow-along feature (=+youtube= flag) — [[id:adr-rss-elfeed][ADR-012]] | =sudo apt install mpv= — done 2026-08-10 (user's own terminal; =sudo= via the =!= session-shell prefix has no TTY for the password prompt, same constraint as Phase 2's mail binaries) | Standard =apt= upgrade | Standard =apt remove=; elfeed-tube's mpv-follow keybindings go inert but elfeed itself is unaffected |
| =yt-dlp= | Video/transcript fetch backend elfeed-tube-mpv calls into, alongside =mpv= — [[id:adr-rss-elfeed][ADR-012]] | =sudo apt install yt-dlp= — done 2026-08-10 (same install run as =mpv=, same constraint) | Standard =apt= upgrade | Standard =apt remove=; same effect as removing =mpv= above |
| =libpoppler-glib-dev= | Build dependency for =pdf-tools='s =epdfinfo= server — [[id:adr-app-launcher][ADR-020]] | Already installed, predates this project's involvement — not installed by this project (agent's own =dpkg -s= check wrongly reported it missing at first; see ADR-020's LOGBOOK) | Standard =apt= upgrade | Standard =apt remove=; =pdf-tools= goes back to unable-to-compile =epdfinfo= |
| ghostel's native module (=libghostty-vt= via a Zig-compiled dynamic module) | Sole terminal backend for claude-code-ide.el and every other terminal use in this config since 2026-08-17, replacing vterm — [[id:adr-terminal-backend-ghostel][ADR-014]] | Elisp package installed via =:term ghostel= (=doom sync=, 2026-08-10); native binary downloaded via =M-x ghostel= (=ghostel-module-auto-install=), confirmed 2026-08-17 — originally under straight's version-stamped build tree. *Relocated 2026-08-29* (=41f5a4d=): the Emacs 30.2 -> 31.1 rebuild orphaned it in =build-30.2/= and killed every terminal path, so =config.el:410= now sets =ghostel-module-directory= to =doom-data-dir= — the module lives at =~/.config/emacs/.local/etc/ghostel/ghostel-module.so= (0.50.0, 1,644,712 bytes), moved rather than re-downloaded and verified to load under 31.1. The orphan copy it left in =build-30.2/ghostel/= was byte-identical (sha256 =e9075a05...=) and went with the whole =build-30.2/= tree when that was deleted 2026-08-29 | Re-run =M-x ghostel-module-compile/-download=; an Emacs major-version bump no longer costs a re-download now that the module sits outside =build-*/= | Delete the downloaded module file and/or remove =ghostel=/=evil-ghostel= from =init.el='s =:term= block, =doom sync= |
| =grip= | Live GitHub-style preview for Markdown/Org — =:lang markdown +grip= ([[id:adr-init-flag-audit][ADR-026]]) | =pipx install grip= (user-level, no root) | =pipx upgrade grip= | =pipx uninstall grip=; drop =+grip= from =init.el= and =doom sync= |
| =jupyter= (jupyterlab + ipykernel) | Kernel/CLI behind =:lang org +jupyter= babel blocks ([[id:adr-init-flag-audit][ADR-026]]) | =pipx install --include-deps jupyterlab= then =pipx inject jupyterlab ipykernel=. =--include-deps= is what exposes the =jupyter= entry point itself — plain =pipx install jupyterlab= only ships =jupyter-lab= | =pipx upgrade jupyterlab= | =pipx uninstall jupyterlab=; drop =+jupyter=, =doom sync=. Note the elisp side also builds a =zmq= native module on first use |
| =texlab=, =marksman= | LaTeX and Markdown language servers for =+lsp= on those modules ([[id:adr-init-flag-audit][ADR-026]]) | Upstream GitHub release binaries, =install -m755= into =~/.local/bin= (no apt package for either) | Re-download the newer release the same way; neither self-updates | =rm ~/.local/bin/{texlab,marksman}=; drop the =+lsp= flag from that module |
| =tuntox= | Tox-protocol port forwarding behind =:tools collab= =+tunnel= ([[id:adr-module-sweep-non-lang][ADR-029]]) | Upstream release binary =tuntox-x64= 0.0.10 (=gjedeer/tuntox=) into =~/.local/bin/tuntox=, =chmod +x= — no apt package. sha256 =76beab684624905ee6a0a913489cb2f03a21f37d800d28e7c4001545a09acb8f= | Re-download the newer release the same way; does not self-update | =rm ~/.local/bin/tuntox=; drop the =+tunnel= flag, =doom sync= |
| =xdotool= (+ =xclip=) | X11 window/keystroke automation =:app everywhere= needs to paste back into the app you invoked it from ([[id:adr-module-sweep-non-lang][ADR-029]]) | =sudo apt install xdotool xclip= — user's own terminal (agent's shell has no TTY for sudo) | Standard =apt= upgrade | Standard =apt remove=; also re-comment =everywhere= in =init.el='s =:app= block and =doom sync= |
| =transmission-daemon= | BitTorrent daemon =transmission.el= drives over RPC on =127.0.0.1:9091= ([[id:adr-torrent-transmission][ADR-030]]) | =sudo apt install transmission-daemon= — done 2026-08-23 (user's own terminal, agent's shell has no TTY for sudo). The packaged *system* unit then fails =Type=notify= startup, so this config runs it as a user unit instead: =~/.config/systemd/user/transmission-daemon.service=, =systemctl --user enable --now transmission-daemon= | Standard =apt= upgrade; the user unit needs no change | =systemctl --user disable --now transmission-daemon=, =rm ~/.config/systemd/user/transmission-daemon.service=, =sudo apt remove transmission-daemon=; drop =transmission= from =packages.el= and =doom sync=. Torrent state persists in =~/.config/transmission-daemon/= unless removed separately |
| =bash-language-server=, =vscode-langservers-extracted= | Shell and JSON language servers for =:lang sh +lsp= / =:lang json +lsp= ([[id:adr-init-flag-audit][ADR-026]]) | =npm install -g --prefix ~/.local <pkg>= — the =--prefix= is deliberate: npm's global prefix here is =/usr/local=, which would need root | =npm install -g --prefix ~/.local <pkg>@latest= | =npm uninstall -g --prefix ~/.local <pkg>=; drop the flag, =doom sync= |
| =atuin= | Shell history search on =C-r=, replacing readline's ([[id:adr-terminal-layer-pass][ADR-033]]). Nothing in Emacs depends on it; it is terminal-only | Download =atuin-x86_64-unknown-linux-musl.tar.gz= and its =.sha256= from the tagged GitHub release, =sha256sum -c=, then =install -m755 atuin ~/.local/bin/=. *Not* apt (no package) and *not* =cargo install= (MSRV 1.97.0 vs this box's rustc 1.75.0). The checksum is served from the same host as the tarball, so it proves transfer integrity, not provenance | Same steps against the newer tag — no self-update. Re-check atuin's =?= AI keybinding on every upgrade: it is disabled by a =bind= in =~/.bashrc= that assumes atuin's current keymap choice, with no config flag to rely on | =rm ~/.local/bin/atuin=; =rm -r ~/.local/share/atuin/= (destroys the imported history database); delete the atuin block from =~/.bashrc= |

*vterm-module build, done once during Phase 6, historical* (=vterm= is
disabled since 2026-08-17, [[id:adr-terminal-backend-ghostel][ADR-014]] — kept here as a reproducible record,
not because it's still needed; the exact commands are captured in
[[id:tshoot-vterm-module][troubleshooting.org]] and reproducible on any
machine with the toolchain above, should =vterm= ever be re-enabled):
#+BEGIN_SRC sh
cd ~/.config/emacs/.local/straight/build-30.1/vterm
mkdir -p build && cd build
cmake -G 'Unix Makefiles' ..
make
#+END_SRC

*Doom's envvar file, regenerated once during Phase 7* (also not
shell-history-only — see [[id:tshoot-daemon-path-systemd][troubleshooting.org]]):
#+BEGIN_SRC sh
~/.config/emacs/bin/doom sync --env
#+END_SRC
Needed on any machine where Emacs (particularly a daemon) might be launched
by something other than an interactive login shell (systemd user session, a
display manager, etc.) — otherwise the daemon's =PATH= may not match the
user's real shell.

* Credential state
:PROPERTIES:
:ID:       inv-credentials
:END:

No secret values recorded here or anywhere in this repo — entry names only.

| Store  | Entries present (2026-08-05) |
|--------+--------------------------------|
| pass   | =api/anthropic=, =api/openai= (confirmed via =pass ls=, 2026-07-21). Phase 2 ([[id:adr-email-mu4e][ADR-011]]): =mail/oauth2-google-client= *confirmed present* (via =pass ls mail=, 2026-08-05), backing the now fully-implemented =personal=/=ualberta= accounts. =mail/tricca= *still not created* — =jcook@tricca.ca= email is therefore *not implemented*, blocked on circumstances outside the user's control (as of 2026-08-05); work is paused here for other priorities as of 2026-08-07, not actively chased. A manual, never-scripted user action once unblocked |
| gpg    | one ultimate-trust secret key (created 2026-02-05, ID =859AF991C9EA4396=), reused as oama's GPG backend key rather than minting a second one |
| oauth2-auto (GPG-backed plstore at =~/.config/emacs/.local/state/oauth2-auto.plist=) | org-gcal's token store, and a *third* credential mechanism alongside =pass= and =oama= — not a choice, it's how org-gcal authenticates ([[id:adr-calendar-org-gcal][ADR-025]]). Reads its client id/secret from the same =mail/oauth2-google-client= pass entry =oama= uses. *Empty as of 2026-08-23*: no authorization has been run, so no token exists yet. The path is set in =config.el=; oauth2-auto's own default would put it inside Doom's git checkout |
| oama (GPG-backed, own store at =~/.local/state/oama=) | Holds per-address OAuth2 refresh/access tokens for the 2 Gmail accounts. Authorized 2026-07-29, live-verified 2026-08-05 morning, then *both went invalid the same afternoon* — =oama access <address>= returned =getEmailAuth: InvalidGrant= for both =mrniceguyjames@gmail.com= and =jcook@ualberta.ca=, exactly 7 days after authorization, consistent with a "Testing" consent-screen 7-day refresh-token cap. *2026-08-06:* user re-=authorize=d both and published the OAuth consent screen (Testing → In production, unverified) to fix the root cause; =oama access <address>= confirmed exit 0 for both immediately after. Durability past the old ~7-day mark (~2026-08-13) not yet confirmed by elapsed time — see [[id:adr-email-mu4e][ADR-011]] Validation/Revisit when. Token output never printed at any point in this diagnosis |

=api/anthropic= was created during Phase 4 (user action) once gptel's
multi-provider rework needed it. =api/gemini= remains intentionally absent —
Google was explicitly skipped, see [[id:adr-gptel-rework][ADR-006]]. See [[id:ai-providers][providers.org]] for the full
credential architecture, and [[id:adr-email-mu4e][ADR-011]] for the mail-specific
=pass=/=oama= split.

* Known environment issues
:PROPERTIES:
:ID:       inv-env-issues
:END:

- =$PATH= contains a literal, unexpanded =~/.config/emacs/bin= entry. Interactive
  bash resolves =doom= via its own tilde-aware command hashing, but any subprocess
  doing a raw =execvp= PATH search (e.g. =timeout doom ...=, and potentially some
  agent tool invocations) fails to find it. Not a Doom or Emacs issue — a shell
  profile issue. Affects anything that shells out to =doom= non-interactively,
  including future agent tool calls. Not yet fixed — out of scope for this
  project unless it starts blocking an agent workflow (see
  [[id:ai-troubleshooting][troubleshooting.org]]).
- The user's real Emacs daemon is launched by the systemd user session, not a
  login shell, so it inherited systemd's bare =PATH= (missing
  =~/.local/bin=, where =claude= lives) until Phase 7 fixed it by running
  =doom sync --env= — see [[id:tshoot-daemon-path-systemd][troubleshooting.org]] and the lifecycle table above.
  Fixed going forward for any new Emacs start; worth re-running =doom sync
  --env= (or =M-x doom/reload-env= on an already-running daemon) any time a
  new binary is added somewhere the daemon's environment wouldn't otherwise
  pick up.

* Reproduction checklist
:PROPERTIES:
:ID:       inv-reproduction-checklist
:END:

Not literally tested on a second machine (none available to this project) —
this is a documentation-completeness pass, not an executed test. Given a
fresh Ubuntu-like machine with Doom Emacs already bootstrapped against this
=$DOOMDIR=, reaching this config's actual working state requires, in order:

1. =doom sync= (picks up =packages.el=: gptel, copilot, claude-code-ide,
   perfect-margin).
2. Install prerequisites this project actually needed: =npm=/=node= (for
   =copilot-language-server=, or accept the native-binary fallback), the
   =claude= CLI (official installer), =pass=+=gpg= with a trusted secret key
   already set up. (=build-essential=+=cmake=, needed to compile vterm's
   native module, is no longer required — =vterm= is disabled since
   2026-08-17, [[id:adr-terminal-backend-ghostel][ADR-014]]; skip unless re-enabling it, see the lifecycle
   table above.)
3. Run =M-x ghostel= interactively once, in a real graphical session, to
   trigger the native =libghostty-vt= module's pre-built-binary download
   (=ghostel-module-auto-install=) — needs an actual interactive session
   with a real X/graphical frame attached, not a headless/frame-less one
   (see [[id:tshoot-ghostel-needs-x-frame][troubleshooting.org]]).
4. =M-x copilot-install-server= (in-Emacs, not a shell step).
5. Create the =api/anthropic= and =api/openai= =pass= entries (manual,
   deliberately never scriptable — see [[id:ai-providers][providers.org]]).
6. Log in / authenticate the =claude= CLI itself (its own subscription login,
   outside this repo's credential system — see [[id:ai-providers][providers.org]]'s two-tier
   credential model).
7. Run =doom sync --env= once, so a daemon started by something other than
   an interactive shell (systemd, a display manager) still gets the right
   =PATH= — see the "Known environment issues" entry above.
8. =doom doctor=, confirm clean.

Known gaps in this checklist, honestly: it's reasoned from what this project
actually did on this one machine, not verified against a second one. Order
dependencies between steps 2-5 haven't been individually stress-tested (e.g.
whether =doom sync= must run before or after the =pass= entries exist).
